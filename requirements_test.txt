pytest==9.1.1
pytest-asyncio
homeassistant==2026.9.2
ruff

# See docs/operations.md "Installing the test harness" for why
# pytest-homeassistant-custom-component isn't pinned in this file.

# Transitive requirements of the homeassistant.components.bluetooth/usb/
# thread/hassio modules this integration imports at module load time; core
# does not install a component's own requirements just because the
# homeassistant package is installed. See docs/operations.md "Bluetooth,
# USB, and Thread test dependencies".
aiousbwatcher==1.1.2
serialx==1.10.0
bleak==3.0.2
bleak-retry-connector==4.7.1
bluetooth-adapters==2.4.0
bluetooth-auto-recovery==1.6.4
bluetooth-data-tools==1.29.24
dbus-fast==5.0.22
habluetooth==6.26.11
aiohasupervisor==0.6.0
python-otbr-api==2.10.0
pyroute2==0.9.6

# aiohomekit is intentionally not pinned here -- install the local fork in
# editable mode instead, see README.md "Development".
